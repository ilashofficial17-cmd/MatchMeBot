[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3.11 -m venv /app/.venv && . /app/.venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
